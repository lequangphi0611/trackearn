services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: trackearn
      POSTGRES_USER: trackearn
      POSTGRES_PASSWORD: trackearn
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
